──────────────────────────────────────────────────
  ◆ Verifpal 1.4.10 · https://verifpal.com
──────────────────────────────────────────────────
 Verifpal ◆ Verification initiated for 'pqtg-relay-handshake-nopin.vp' at 10:13:02 AM.
     Info ● Attacker is configured as active.
     Info ● Running at phase 0.
     Fail ✗ confidentiality? key_s2c_s (Analysis 4)
     Fail ✗ confidentiality? key_c2s_s (Analysis 4)
     Fail ✗ authentication? Client -> Server: kem_ek (Analysis 4)
     Fail ✗ confidentiality? key_c2s_c (Analysis 17)
     Fail ✗ confidentiality? key_s2c_c (Analysis 17)
     Fail ✗ authentication? Server -> Client: transcript_sig (Analysis 24)

──────────────────────────────────────────────────
 Verifpal ◆ Verification completed for 'pqtg-relay-handshake-nopin.vp' at 10:13:02 AM in 82ms.

     Fail ✗ confidentiality? key_s2c_s
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces kem_ek (sent by Client to Server) with PUBKEY(nil). (kem_ek was PUBKEY(kem_sk))
            │ 3. Attacker observes KEM_ENCAP(PUBKEY(nil), kem_r)|2 on the wire.
            │ 4. Attacker opens KEM_ENCAP(PUBKEY(nil), kem_r)|2 with nil, obtaining KEM_ENCAP(PUBKEY(nil), kem_r)|1.
            │ 5. Attacker observes client_random on the wire.
            │ 6. Attacker observes server_random on the wire.
            │ 7. Attacker constructs HASH(client_random, server_random, PUBKEY(nil)).
            │ 8. Attacker observes client_pk on the wire.
            │ 9. Attacker observes server_pk on the wire.
            │ 10. Attacker constructs HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk).
            │ 11. Attacker constructs HASH(label_s2c, KEM_ENCAP(PUBKEY(nil), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk)).
            ╰▸ key_s2c_s (HASH(label_s2c, KEM_ENCAP(PUBKEY(nil), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk))) is obtained by Attacker.
     Fail ✗ confidentiality? key_c2s_s
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces kem_ek (sent by Client to Server) with PUBKEY(nil). (kem_ek was PUBKEY(kem_sk))
            │ 3. Attacker observes KEM_ENCAP(PUBKEY(nil), kem_r)|2 on the wire.
            │ 4. Attacker opens KEM_ENCAP(PUBKEY(nil), kem_r)|2 with nil, obtaining KEM_ENCAP(PUBKEY(nil), kem_r)|1.
            │ 5. Attacker observes client_random on the wire.
            │ 6. Attacker observes server_random on the wire.
            │ 7. Attacker constructs HASH(client_random, server_random, PUBKEY(nil)).
            │ 8. Attacker observes client_pk on the wire.
            │ 9. Attacker observes server_pk on the wire.
            │ 10. Attacker constructs HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk).
            │ 11. Attacker constructs HASH(label_c2s, KEM_ENCAP(PUBKEY(nil), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk)).
            ╰▸ key_c2s_s (HASH(label_c2s, KEM_ENCAP(PUBKEY(nil), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(nil)), KEM_ENCAP(PUBKEY(nil), kem_r)|2, client_pk, server_pk))) is obtained by Attacker.
     Fail ✗ confidentiality? key_c2s_c
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces server_pk (sent by Server to Client) with PUBKEY(nil). (server_pk was PUBKEY(server_sk))
            │ 3. Attacker replaces kem_ek (sent by Client to Server) with PUBKEY(nil). (kem_ek was PUBKEY(kem_sk))
            │ 4. Attacker observes KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2 on the wire.
            │ 5. Attacker replays kem_ct (Server to Client) unaltered: KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2 is what Server sent.
            │ 6. Attacker observes PUBKEY(kem_sk) on the wire.
            │ 7. Attacker observes KEM_ENCAP(PUBKEY(nil), kem_r)|2 on the wire.
            │ 8. Attacker opens KEM_ENCAP(PUBKEY(nil), kem_r)|2 with nil, obtaining kem_r.
            │ 9. During Client's run (phase 0), attacker constructs KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1.
            │ 10. Attacker observes client_random on the wire.
            │ 11. Attacker observes server_random on the wire.
            │ 12. Attacker constructs HASH(client_random, server_random, PUBKEY(kem_sk)).
            │ 13. Attacker observes client_pk on the wire.
            │ 14. Attacker constructs HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil)).
            │ 15. During Client's run (phase 0), attacker constructs HASH(label_c2s, KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil))).
            ╰▸ key_c2s_c (HASH(label_c2s, KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil)))) is obtained by Attacker.
     Fail ✗ confidentiality? key_s2c_c
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces server_pk (sent by Server to Client) with PUBKEY(nil). (server_pk was PUBKEY(server_sk))
            │ 3. Attacker replaces kem_ek (sent by Client to Server) with PUBKEY(nil). (kem_ek was PUBKEY(kem_sk))
            │ 4. Attacker observes KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2 on the wire.
            │ 5. Attacker replays kem_ct (Server to Client) unaltered: KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2 is what Server sent.
            │ 6. Attacker observes PUBKEY(kem_sk) on the wire.
            │ 7. Attacker observes KEM_ENCAP(PUBKEY(nil), kem_r)|2 on the wire.
            │ 8. Attacker opens KEM_ENCAP(PUBKEY(nil), kem_r)|2 with nil, obtaining kem_r.
            │ 9. During Client's run (phase 0), attacker constructs KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1.
            │ 10. Attacker observes client_random on the wire.
            │ 11. Attacker observes server_random on the wire.
            │ 12. Attacker constructs HASH(client_random, server_random, PUBKEY(kem_sk)).
            │ 13. Attacker observes client_pk on the wire.
            │ 14. Attacker constructs HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil)).
            │ 15. During Client's run (phase 0), attacker constructs HASH(label_s2c, KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil))).
            ╰▸ key_s2c_c (HASH(label_s2c, KEM_ENCAP(PUBKEY(kem_sk), kem_r)|1, HASH(HASH(client_random, server_random, PUBKEY(kem_sk)), KEM_ENCAP(PUBKEY(kem_sk), kem_r)|2, client_pk, PUBKEY(nil)))) is obtained by Attacker.
     Fail ✗ authentication? Server -> Client: transcript_sig
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces server_pk (sent by Server to Client) with PUBKEY(nil). (server_pk was PUBKEY(server_sk))
            │ 3. Attacker observes client_random on the wire.
            │ 4. Attacker observes server_random on the wire.
            │ 5. Attacker observes kem_ek on the wire.
            │ 6. During Server's run (phase 0), attacker constructs HASH(client_random, server_random, kem_ek).
            │ 7. Attacker observes client_pk on the wire.
            │ 8. Attacker constructs HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil)).
            │ 9. Attacker constructs SIGN(nil, HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil))).
            │ 10. Attacker replaces kem_ct, transcript_sig (sent by Server to Client) with nil, SIGN(nil, HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil))). (kem_ct was KEM_ENCAP(kem_ek, kem_r)|2; transcript_sig was SIGN(server_sk, transcript_s))
            │ 11. Client's SIGNVERIF(PUBKEY(nil), HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil)), SIGN(nil, HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil))))? passes — the attacker controls one of its inputs.
            ╰▸ transcript_sig (SIGN(nil, HASH(HASH(client_random, server_random, kem_ek), nil, client_pk, PUBKEY(nil)))), sent by Attacker and not by Server, is successfully used in SIGNVERIF(server_pk, transcript_c, transcript_sig)? within Client's state.
     Fail ✗ authentication? Client -> Server: kem_ek
            ╭─ Attack trace:
            │ 1. Attacker constructs PUBKEY(nil).
            │ 2. Attacker replaces kem_ek (sent by Client to Server) with PUBKEY(nil). (kem_ek was PUBKEY(kem_sk))
            │ 3. Attacker observes KEM_ENCAP(PUBKEY(nil), kem_r)|2 on the wire.
            │ 4. Attacker opens KEM_ENCAP(PUBKEY(nil), kem_r)|2 with nil, obtaining KEM_ENCAP(PUBKEY(nil), kem_r)|1.
            ╰▸ kem_ek (PUBKEY(nil)), sent by Attacker and not by Client, is successfully used in KEM_ENCAP(kem_ek, kem_r)|1 within Server's state.

──────────────────────────────────────────────────
     Fail ✗ 6 of 6 queries failed.
 Verifpal ◆ Thank you for using Verifpal.
